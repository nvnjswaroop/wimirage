-r requirements.txt
#
# Section 8 #7 + #8 — CI deps for vulnerability + license auditing.
# Section 7 #7 — mutation testing.
#
pip-audit>=2.7,<3.0
pip-licenses>=5.0,<6.0
mutmut>=3.0,<4.0
#
# Test framework + coverage. (Section 7 final: tests in real CI.)
#
pytest>=8.0.0,<9.0
pytest-cov>=5.0.0,<6.0
pytest-mock>=3.12.0,<4.0
pytest-benchmark>=4.0.0,<6.0
#
# Property-based fuzz testing (tests/test_validators_fuzz.py).
#
hypothesis>=6.0,<7.0
#
# Lint + type-check.
#
ruff>=0.3.0,<0.5.0
mypy>=1.9.0,<2.0
types-flask>=3.0.0
types-jinja2>=2.11.0
#
# Lockfile regeneration (only needed by maintainers; install via
# `pip install -r requirements-dev.txt` if you want to run pip-compile).
#
pip-tools>=7.0,<8.0
