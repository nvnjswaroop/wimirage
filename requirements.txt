# Core runtime dependencies (always required).
scapy>=2.5.0,<3.0
flask>=3.0.0,<4.0

# -----------------------------------------------------------------------------
# Optional extras (install only what you need):
#
#   pip install wimirage[encrypt]   # Fernet-encrypted credential log
#   pip install wimirage[sms]       # Twilio SMS OTP delivery
#   pip install wimirage[dev]       # pytest + ruff + mypy + coverage
#   pip install wimirage[all]       # every extra above
# -----------------------------------------------------------------------------

# cryptography>=42.0.0,<44.0     is provided by [encrypt]
# twilio>=8.0.0,<9.0              is provided by [sms]
